# Procedimento para se fazer uma instalação limpa.

# limpando pasta de dados
rm -rf app_data/*

# limpando todo lixo do docker
# Recomenda-se usar o comando do Docker para limpar contêineres não utilizados. Contêiner, redes, imagens, e o cache criado podem ser limpos executando o seguinte comando:
docker system prune -a

# importando modelo de tabelas
docker exec -i mariadb sh -c 'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD"' < database.sql


backlog
